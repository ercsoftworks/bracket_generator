<UserControl Name="UC" x:Class="Brackets2012.MetroButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="300" Height="167">
    <Grid x:Name="ButtonGrid">
<Button Background="{Binding Background, ElementName=UC}" Margin="0,0,0,0">
            <StackPanel Orientation="Vertical" Height="{Binding ElementName=ButtonGrid}" Width="{Binding ElementName=ButtonGrid}">
                <Image Source="{Binding Image, ElementName=UC}" Width="80" Height="58" VerticalAlignment="Top" />
                <TextBlock Text="{Binding Text, ElementName=UC}" Width="Auto"  FontSize="20" FontFamily="Segoe WP" Height="42" VerticalAlignment="Top" HorizontalAlignment="Center" Foreground="#FFFFFDFD" />
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
